package kata

import ( "unicode" )

type MyString string

func (s MyString) IsUpperCase() bool {
  for _, char := range s {
    if !unicode.IsUpper(char) && unicode.IsLetter(char) {
      return false
    }
  }
  return true
}